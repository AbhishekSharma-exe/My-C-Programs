#include<stdio.h>
int main()
{
    char a;
    printf("Enter the character to know its ASCII value:");
    scanf("%c",&a);
    printf("The ASCII value of %c Character is :%d ",a,a);
    return 0;
}